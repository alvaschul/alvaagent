# Python
__pycache__/
*.pyc

# Node (only needed for the optional Pyodide test harness)
node_modules/
package-lock.json

# Local data / runtime state
.alvaagent/store.json
.alvaagent/config.json
.alvaagent/cmd_history.txt
*.log

# Secrets / runtime state (tracked by mistake earlier — now untracked)
.alvaagent/config.json
.alvaagent/store.json
.alvaagent/cmd_history.txt

# Local opencode session config (not part of the project)
.opencode/
opencode.json

# SDD planning workspace (ledger, task briefs, review packages)
.superpowers/
